/* eslint-disable @next/next/no-img-element */
import React, { useState } from "react";

interface AcademicsProps {
  name: string;
}


function ActivitiesSignUp(props: any) {
  return (
    <div>
        <button>Add Activities</button>
    </div>
  );
}

export default ActivitiesSignUp;
